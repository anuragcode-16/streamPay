# Pulse Pay – Example Environment Variables
#
# Copy the relevant parts into:
#   - Frontend:  .env          (in repo root, used by Vite)
#   - Backend:   server/.env   (inside the server folder, used by Express)
#
# Do NOT commit real secrets. Replace the placeholder values below.

############################################
# Frontend (Vite React app)
############################################

# Base URL for the Pulse Pay backend (Express + Socket.IO)
VITE_API_URL=http://localhost:4000

# Supabase project (Auth + profiles)
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_SUPABASE_ANON_OR_PUBLIC_KEY

# Razorpay Test Mode browser key (starts with rzp_test_)
VITE_RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID

############################################
# Backend (server/.env – Node + Express)
############################################

# Postgres / Supabase connection string
# Example (Supabase):
# DATABASE_URL=postgresql://postgres:POSTGRES_PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME

# Razorpay Test Mode (from Dashboard → Settings → API Keys)
RAZORPAY_KEY_ID=rzp_test_YOUR_KEY_ID
RAZORPAY_KEY_SECRET=YOUR_TEST_KEY_SECRET

# Razorpay Webhook Secret (Dashboard → Settings → Webhooks)
RAZORPAY_WEBHOOK_SECRET=YOUR_WEBHOOK_SECRET

# Express server config
PORT=4000
FRONTEND_URL=http://localhost:5173

# Streaming tick interval (per-second billing)
# 1000 ms for demo (1s), 5000 ms for lighter load, etc.
TICK_INTERVAL_MS=1000

# Optional: enable basic SQL logging
DEBUG_SQL=false

############################################
# OpenStreetMap / Maps (optional overrides)
############################################

# Tile URL template (defaults to openstreetmap.org if unset)
# OSM_TILE_URL=https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png

# Nominatim / Overpass endpoints if you want to point to your own instance
# OSM_NOMINATIM_URL=https://nominatim.openstreetmap.org/search
# OSM_OVERPASS_URL=https://overpass-api.de/api/interpreter


